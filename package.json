{
  "name": "cv",
  "private": false,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && git add dist -f && git commit --amend --no-edit && git push origin `git subtree split --prefix dist master`:gh-pages --force"
  },
  "devDependencies": {
    "vite": "^2.9.0"
  }
}
