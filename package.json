{
  "name": "vite-starter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && git add dist -f && git commit --amend --no-edit && git subtree push --prefix dist origin gh-pages  "
  },
  "devDependencies": {
    "vite": "^2.9.0"
  },
  "dependencies": {}
}
